version: '3.8'

networks:
  psf-network:
    external: true

services:
  iaminy:
    build: .
    container_name: psf-app-iaminy
    restart: always
    ports:
      - "8765:8765"
    networks:
      - psf-network
